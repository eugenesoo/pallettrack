version: "3"
services:
  db:
    image: postgres:latest
    ports:
      - "5432:5432"
    restart: always
    environment:
      POSTGRES_PASSWORD: nyancat
  web:
    image: web:latest
    ports:
      - "1337:1337"
    restart: always
    working_dir: /home/node/app
